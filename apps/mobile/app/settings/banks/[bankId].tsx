import { RoutePlaceholder } from '../../../src/components/RoutePlaceholder';

export default function BankReview() {
  return <RoutePlaceholder screenId="bank-review" route="/settings/banks/[bankId]" />;
}
