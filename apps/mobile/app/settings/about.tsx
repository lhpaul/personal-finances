import { RoutePlaceholder } from '../../src/components/RoutePlaceholder';

export default function SettingsAbout() {
  return <RoutePlaceholder screenId="settings-about" route="/settings/about" />;
}
