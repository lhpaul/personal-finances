/// <reference types="expo/types" />

// NOTE: committed rather than gitignored (Decision 8): `experiments.typedRoutes` stays off
// in this item, so there is no generated `.expo/types` output for CI's `tsc --noEmit` job to
// depend on. This file must exist from a clean clone for type-checking to be hermetic.
